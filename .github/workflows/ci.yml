name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_HOST_AUTH_METHOD: trust
        ports:
          - 5433:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10

    env:
      TEST_DATABASE_URL: postgres://postgres@localhost:5433/fusion_test

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      # Native build dependencies for the `canvas` package
      - name: Install system dependencies
        run: |
          sudo apt-get update -q
          sudo apt-get install -y -q libcairo2-dev libpango1.0-dev libjpeg-dev libgif-dev librsvg2-dev pkg-config

      - name: Install dependencies
        run: npm ci

      - name: Type check
        run: npm run check

      - name: Create test database
        run: |
          psql "postgres://postgres@localhost:5433/postgres" -c "CREATE DATABASE fusion_test"
          DATABASE_URL="$TEST_DATABASE_URL" npx drizzle-kit migrate

      - name: Run tests
        run: npm test

      - name: Build
        run: npm run build
